export enum ActionTypes {
  LOADS_BOOK = '[Books Effect] Books Loaded Successfully',
  CREATE_BOOK = '[Create Book Component] Create Book',
  DELETE_BOOK = '[Book Effect] Delete Book',
  UPDATE_BOOK = '[Books Effect] Update Book',
  SET_BOOK = '[Books Effect] Get Book',
}
